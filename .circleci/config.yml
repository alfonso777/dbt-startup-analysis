---
version: 2.1
parameters:
  workflow:
    type: enum
    enum:
      - w1
      - w2
    default: w1
jobs:
  build-and-test:
    docker:
      - image: busybox
    steps:
      - run:
          name: Run tests
          command:echo 'Parameter workflow: <<parameters.workflow>>'
workflows:
  sample:
    jobs:
      - build-and-test
