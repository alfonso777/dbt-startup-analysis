version: 2

models:
   - name: xpto1
    description: pending
    columns:
      - name: id
        tests:
          - unique
          - not_null



  - name: xpto2
    description: pending
    columns:
      - name: id
        description: pending